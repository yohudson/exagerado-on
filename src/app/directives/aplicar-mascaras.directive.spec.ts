import { AplicarMascarasDirective } from './aplicar-mascaras.directive';

describe('AplicarMascarasDirective', () => {
  it('should create an instance', () => {
    const directive = new AplicarMascarasDirective();
    expect(directive).toBeTruthy();
  });
});
